[Unit]
Description=Sentinel API
After=network.target

[Service]
WorkingDirectory=/opt/sentinel/backend
ExecStart=/opt/sentinel/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target

