# Evaluation

Each training run writes `reports/{run_id}/metrics.json`, charts, a confusion matrix, feature importance, and an auto-generated markdown report.

Read PR curves before ROC curves. For this domain, a high ROC-AUC can still hide weak precision at usable recall levels.

