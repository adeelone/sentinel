# Contributing

Run the local checks before opening a change:

```bash
make lint
make typecheck
make test
```

Keep evaluation claims honest. Do not promote accuracy as a primary metric, and do not add examples containing real cardholder data.

